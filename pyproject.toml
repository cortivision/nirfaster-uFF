[project]
name = "nirfasteruff"
version = "1.2.1"
dependencies = []

[tool.black]
line-length = 100
target-version = ['py313']
exclude = '''
(
  /(
      \.eggs
    | \.git
    | \.mypy_cache
    | \.tox
    | \.venv
    | build
    | dist
  )/
)
'''

[tool.setuptools.packages.find]
where = ["."]
